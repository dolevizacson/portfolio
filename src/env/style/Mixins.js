import { css } from 'styled-components';

export const navbarSeparator = () => css`
  border-bottom: none;
  box-shadow: var(--ui-shadow-1);
`;

export const centerContent = () => css`
  display: flex;
  justify-content: center;
  align-items: center;
`;

export const formModalStyle = () => css`
  text-align: center;

  font-size: 1.8rem;
  font-weight: 500;
`;
